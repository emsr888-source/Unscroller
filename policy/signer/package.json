{
  "name": "@unscroller/policy-signer",
  "version": "1.0.0",
  "main": "dist/index.js",
  "bin": {
    "policy-sign": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "sign": "npm run build && node dist/cli.js sign --input ../policy.json --output ./signed-policy.json --public-key ./public-key.pem"
  },
  "dependencies": {
    "@unscroller/policy-engine": "*",
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
